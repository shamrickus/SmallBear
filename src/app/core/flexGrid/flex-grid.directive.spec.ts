import { FlexGridDirective } from './flex-grid.directive';

describe('FlexGridDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexGridDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
